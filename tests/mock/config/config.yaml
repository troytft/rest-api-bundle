framework:
    validation:
        enabled: true
        enable_annotations: true
    router:
        resource: "%kernel.root_dir%/config/routing.yaml"
        strict_requirements: ~
        type: yaml
